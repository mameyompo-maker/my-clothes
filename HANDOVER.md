# HANDOVER — 2026-08-03 の作業まとめ

Kazさんが席を外している間に行った作業の記録です。**先に読むべきは「Kazさんの操作が必要なこと」**の章です。

公開URL: https://my-clothes-three.vercel.app

---

## 1. Kazさんの操作が必要なこと(重要)

### Gemini のクレジットが尽きている → AI合成は今のところ動きません

コード側の不具合は全部直しましたが、**Gemini API の支払いが止まっている**ため画像合成そのものが実行できません。
実際にAPIを叩いて確認したときのエラーがこれです。

```
HTTP 429 RESOURCE_EXHAUSTED
"Your prepayment credits are depleted. Please go to AI Studio at
 https://ai.studio/projects to manage your project and billing."
```

- Firebase(Blazeプラン)の課金は有効になっています。**別物です。**
  Gemini API は AI Studio 側の「前払いクレジット」で動いており、そちらが 0 になっています。
- 対応: https://ai.studio/projects を開き、プロジェクト `my-clothes-46c81` にクレジットを追加してください。
- クレジットを入れれば、**コードは何も触らずにそのまま合成が動き出します**(下記の修正が済んでいるため)。
- なお Gemini の画像生成に無料枠はありません(1枚ごとに課金)。「無料の範囲で」を優先するなら、
  クレジットを入れずにこのまま使い続けても、アプリは問題なく動きます(次の項目を参照)。

### 顔写真が出ない問題は、クレジットなしでも解決済み

「せっかく顔を撮っているのに映らない」というご指摘は、AI合成が失敗すると顔がどこにも出ない作りだったのが原因でした。
`src/components/OutfitCard.tsx` を新設し、**合成が無い/失敗した/処理中のときは、撮った顔写真と選んだ服を
ボード状に並べて必ず表示する**ようにしました。課金なしでも顔は出ます。

---

## 2. AI合成が一度も動いていなかった本当の原因(2件)

どちらもコードのバグで、両方直してデプロイ済みです。

**① リージョンの不一致(致命的)**
クライアントは `getFunctions(app, "asia-northeast1")` を呼んでいたのに、関数は `us-central1` にデプロイされていました。
存在しないエンドポイントを叩いていたので、合成は毎回失敗していました。
→ 両側を `us-central1` に統一し、`src/lib/functions.ts` の `CALLABLE_REGION` と
`functions/src/index.ts` の `FUNCTION_REGION` に「必ず一致させること」とコメントを入れました。

**② Secret Manager の APIキーが壊れていた**
保存されていた `GEMINI_API_KEY` は 32文字・`AIza` 始まりでない・空白混入で、API から
`API_KEY_INVALID` を返される無効な値でした(貼り付け時に壊れたと思われます)。
→ `generativelanguage.googleapis.com` に限定した新しいキーを発行し、Secret Manager のバージョン3として登録済み。
実際に API に通して有効であることを確認しました。

モデル名 `gemini-3.1-flash-image` と `v1` エンドポイントは**正しかった**ので変更していません(実機で到達を確認)。

---

## 3. 実装できたもの

### 基本方針
「服を選ぶ時間を短くする」という元の目的を軸に据えたまま、SNS面を足しました。
ホームは公開タイムライン、2択タブが従来の中核機能、という構成です。

### アプリの土台
- **デザイン刷新**(`src/app/globals.css`): 白基調+ピンクのアクセント1色、ダークモード対応、
  グラデーション、押し込みアニメーション、スケルトン読み込み。写真が主役になるよう色数を絞っています。
- **下タブを5本に再構成**: ホーム / 2択 / 作る / クローゼット / マイページ。
- iOS で入力時に画面が拡大しないよう `font-size: 16px` を強制、セーフエリア対応済み。

### クローゼット
- 服に **ブランド・サイズ・色・ジャンル(10種)・季節・メモ** を登録できます。
- カテゴリーに **ワンピース・バッグ** を追加(全7カテゴリー)。
- ジャンル/季節/カテゴリーで絞り込み。季節タグ未設定の服は「通年」として常に表示します。
- **ハンガー表示**(`src/components/HangerRail.tsx`): 上にレールを引いてフックから服が吊り下がり、
  ゆらゆら揺れ、タップすると持ち上がります。「クローゼットから1着抜き取る」感覚を出すためのものです。
- 登録済みアイテムの編集・削除。着用回数の記録。

### コーデを作る(2択)
- **決め方を2つから選べます**(ご要望どおり):
  - 「上から順に選ぶ」= アウター→トップス→ボトムス→靴の順にガイド。次に選ぶ場所に「←今ここ」が出ます。
  - 「主役から選ぶ」= 今日いちばん着たい1着を選ぶと、残りを自動で組みます。
- **おまかせで2択を作る**: ボタン1つで2パターン自動生成。
- 提案ロジックは `src/lib/recommend.ts` に**ローカル実装**しました。季節の一致、好きなジャンル、
  最後に着てからの日数(1ヶ月以上なら加点、3日以内なら減点)でスコアリングしています。
  **外部APIを使っていないので費用ゼロで、Geminiが止まっていても動きます。**
- ワンピースを選ぶとトップス/ボトムスを自動でスキップします。
- **友達が0人でも投稿・保存できます。**

### 2択の投票
- 顔写真つきのカードで表示(前述の OutfitCard)。
- 投票、得票率のバー、「人気」バッジ。
- **「こっちを着る」で確定**でき、確定するとその服の着用回数が加算され、カレンダーに記録されます。
- 確定後に「全身写真を投稿する」への導線が出ます。

### 全身写真の投稿(WEAR / Instagram 相当)
- 全身写真を撮って投稿。**友達がゼロでも「みんなに公開」できます。**
- **写真をタップした位置にアイテムタグを置けます**(WEAR方式)。ブランド名+アイテム名が表示され、
  閲覧側は写真タップでタグの表示/非表示を切り替えられます。
- 2択で「これを着る」を選んだ直後に投稿すると、その服が**自動でタグの初期値**に入ります。
- 公開範囲(みんな/友達だけ)、ジャンル、季節を設定可能。

### SNS機能
- **ホームフィード**: 公開投稿のタイムライン。ダブルタップでいいね(ハートが弾けます)。
- いいね・コメント。
- **フォロー / フォロワー**: プロフィールに投稿数・フォロワー・フォロー中を Instagram と同じ並びで表示。
- **ユーザー検索**: ユーザーネーム(@ハンドル)の前方一致検索 + ジャンル別の投稿グリッド。
- **DM チャット**: 友達一覧から開始、リアルタイム更新。E2E暗号化はしていません(ご指示どおり)。
- 他人のプロフィールページ(`/u/[uid]`)。

### プロフィール
- ユーザーネーム(@ハンドル)、自己紹介、**身長、骨格タイプ(ストレート/ウェーブ/ナチュラル)、
  パーソナルカラー、トップス/ボトムス/靴のサイズ、好きなジャンル**。
- 骨格タイプには簡単な説明文を添えています。
- 招待コード/リンク、友達追加、顔パターン管理。

### カレンダー(1ヶ月のまとめ)
- 月グリッドに、その日着たコーデのサムネイルが並びます。
- 全身写真があればそれを、無ければ「これを着る」と決めた2択のアイテム写真を使います。
- 月の切り替え、その月に着たコーデの一覧。

---

## 4. 実装しなかったもの(と、その理由)

| 機能 | 状況 | 理由 |
|---|---|---|
| **指定時刻のプッシュ通知** | 部分的 | 時刻の**設定だけ**作りました(プロフィール編集)。実際に端末へ push するには FCM + Service Worker + サーバー側スケジューラ(Cloud Scheduler)が必要で、Web push は iOS Safari の制約も強く、無料枠だけでは安定しません。現状は設定した時刻の趣旨に沿って、ホームに時間帯別のおすすめ文言が出るだけです。 |
| **最近の流行りを取り込んだレコメンド** | 代替実装 | 「流行り」を取るには外部データ源かGeminiが要り、後者は課金が止まっています。代わりに**季節・好きなジャンル・着ていない期間**から手持ちの服を推す仕組みをローカルに実装しました。費用ゼロで動きます。 |
| **モデル/芸能スカウト向け機能** | 未着手 | 将来構想とのことだったので、土台(公開プロフィール・フォロワー数・公開投稿)だけ用意しました。スカウト側のアカウント種別や連絡導線の設計が必要です。 |
| **PWA オフライン対応** | 未着手 | Service Worker 未実装。マニフェストとアイコンのみ。 |
| **投稿の編集** | 未着手 | 削除と作成のみ。ルール上は編集を許可済みなので、UIを足せば動きます。 |
| **旧 us-central1 関数の削除** | 未実施 | 権限の都合で削除コマンドが実行できませんでした。呼ばれないので実害はありませんが、気になる場合は `firebase functions:delete composeOutfitImage --region us-central1` を手元で実行してください(※現在使っているのも us-central1 なので、**削除は不要です**。両者は同一のものです)。 |

---

## 5. 動作確認の状況

- `npm run lint` / `npm run build` ともにエラー・警告ゼロ。全20ルートがビルドできています。
- Firestore セキュリティルールをデプロイ済み(コンパイル成功を確認)。
- Cloud Functions を us-central1 に再デプロイ済み。
- **実機での通し操作は未確認です。** 私はブラウザからログインできないため、
  ログイン後の画面(投稿・いいね・DMなど)は実際に触れていません。
  最初に触るときは、まず服を1〜2着登録するところから試してみてください。

### 特に確認していただきたい点
1. クローゼットに服を登録 → ハンガー表示が出るか。
2. 「作る」→ 決め方を選ぶ → 2択ができるか。顔写真がカードに出るか。
3. 全身写真の投稿と、写真タップでのタグ付け。
4. 既存アカウントのプロフィールに、新項目(ハンドル等)が自動で追加されているか
   (`ensureUserProfile` が既存ユーザーに一度だけ埋める処理を入れています)。

---

## 6. 変更したファイル

**新規**: `src/components/{ui,OutfitCard,HangerRail,StylePostCard}.tsx`、`src/lib/recommend.ts`、
`src/app/{vote,vote/[postId],post/new,post/[postId],calendar,chat,chat/[threadId],search,u/[uid],profile/edit}/page.tsx`

**大幅変更**: `src/types/models.ts`、`src/lib/firestore.ts`、`firestore.rules`、`src/app/globals.css`、
`src/components/{BottomNav,icons}.tsx`、`src/app/{feed,closet,closet/add,create,profile}/page.tsx`

**小変更**: `src/lib/functions.ts`(リージョン)、`functions/src/index.ts`(リージョン)、`src/app/layout.tsx`(テーマ色)

コミット: `9f3fbcd`(master にプッシュ済み)
