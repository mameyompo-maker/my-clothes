# HANDOVER — my_clothes(ファッションコーデアプリ)

最終更新: 2026-08-05 / 前セッションからの引き継ぎメモ。**新しいセッションを始めたらこれを最初に読むこと。**

> ## 🚨 2026-08-05 に判明した最重要事項(必ず読むこと)
>
> **Firestore の索引(`firestore.indexes.json`)が1つも作られていなかった。**
> そのため次の2本のクエリが**サーバー側でずっと失敗していた**。
>
> | 失敗していたクエリ | 影響していた画面 |
> |---|---|
> | `stylePosts` visibility== + orderBy createdAt | ホーム/さがすの**公開タイムライン全体** |
> | `outfitPosts` ownerUid== + orderBy createdAt | 2択一覧の**「あなたの2択」** |
>
> さらに `collectionGroup(saves).where(uid)` も索引が無く、「保存した投稿」は
> 一度も引けていなかった。
>
> **これが「遅い」の正体だった。** `onSnapshot` にエラーコールバックを渡していなかったため、
> 失敗しても例外がどこにも出ず、画面上は「いつまでも読み込み中」としてしか観測できなかった。
> 索引を定義してデプロイし、**全購読にエラーコールバック(`snapshotFailed`)を付けた**。
>
> あわせて、**Storage の画像が `Cache-Control: private, max-age=0` で配信されていた**
> (=ブラウザが一切キャッシュしない)ことも実測で判明。画面を移動して戻るたびに
> 全写真を再ダウンロードしていた。`uploadImage` に `immutable` を付け、既存27件も
> `scripts/backfill_cache_control.py` で塗り直し済み。
>
> **教訓: 「遅い」と報告されたら、まず実データでクエリを叩いて成否を確かめること。**
> 体感の遅さと、そもそも失敗していることは、画面上は区別が付かない。

> ### 次のセッションの始め方
>
> Kazさんは新しいセッションで、次のように話しかければよい:
>
> > 「my_clothes の続き。HANDOVER.md を読んで、まず未回答の質問と実機確認から」
>
> **作業は全て push 済みで、未コミットの変更は無い**(`cloth_image/` だけは意図的に
> 追跡していない。アプリが使うのは `public/seed/` のコピーなので重複を避けている)。
> したがって、いきなり実装に入って問題ない状態になっている。
>
> **2026-08-05 に入れたもの**: 索引の追加(=ホームと2択一覧の表示がそもそも
> 直った)、画像キャッシュの修正、往復回数の削減、公式アカウントのプレビュー導線。
> 詳しくは 4.11 / 4.12 と、確認項目15。
>
> **Kazさんへの未回答の質問(2026-08-04 第3弾の締めで提示済み)**:
> 1. 写真2択(全身写真の候補)を**無料のままにするか、プレミアム限定にするか**
>    (現状は無料。有料化は create/page.tsx の破線ボックスを isPremium で分岐するだけ)
> 2. **公式サンプル6人の投稿に気温(tempC)を焼き込んで再投入するか**
>    (ホームの「🌡今日の気温に近い」フィルタが初日から機能するようになる。
>    scripts/seed_demo_accounts.py に tempC を足して再実行するだけ)
> 3. 地域(placeName)での絞り込みを作るか(人が増えてからで良い、が現時点の判断)
> 4. クローゼットの「コーデ」タブ等のデザイン素案・参考画像があるか(あれば寄せる)
>
> ほかに **Discordプラグインの許可リストが外れている**(2026-08-04 夕方から
> `reply` が「channel not allowlisted」で失敗)。Kazさんが `/discord:access` で
> 復旧するまでは `tools/discord_notify.py`(第2経路)で通知すること。

- 公開URL: https://my-clothes-three.vercel.app
- リポジトリ: https://github.com/mameyompo-maker/my-clothes (master 直押し運用)
- ローカル: `C:\Users\kazdr\dev\my_clothes` (**OneDrive外のローカルディスク**。同期されていない)
- Firebase プロジェクト: `my-clothes-46c81`
- 最新コミット: `c6d3f3b`(画像のキャッシュ修正。2026-08-05 第5弾)
  - 同日それ以前: `dab25be` 2択まわりの読み取り集約 / `202bc4c` **索引の追加と全面高速化・公式プレビュー**
  - 2026-08-04: `56913d3` 着用画・PC2色・フォロー基準 / `f7c4d64` 要望リスト一括対応 /
    `3364aaf` 公開2択の投票結果ルール修正 / `2c1225c` 公式サンプル6人 /
    `1ad701a` カルーセル・2択コメント / `9c5eff5` 値段・先行合成・理由スタンプ / `478440a` DM既読・時刻・写真

---

## 0. まず把握すべき現状

| 項目 | 状態 |
|---|---|
| アプリ本体 | 稼働中。全24ルート、lint/build ともにエラーなし |
| Gemini AI合成 | 稼働中。**各利用者が自分のAPIキーを登録して使う方式**(2026-08-06 変更。上の🔑の節)。運営のキーは不使用 |
| 課金(Stripe) | テストモードで決済・解約まで**実機確認済み**。本番モードへの移行が未 |
| 特定商取引法の表記 | 記入済み(月額330円 税込) |
| Cloud Functions | `composeOutfitImage` / `precomposeOutfit` / Stripe系3関数ともデプロイ済み(すべて `us-central1`) |
| デザイン | 白基調 + **青 `#3b6bff`**。クローゼットは木製ワードローブのカルーセル表示(Kazさん指定画像準拠) |
| SNS機能 | 天気連動・ハッシュタグ・ブロック・通報・おすすめ・2択公開・通知・保存・共有(4.7/4.8)+ DM既読/写真・値段・理由スタンプ・2択コメント・グループ共有(すべて2026-08-04) |
| 時短の中核 | 保存コーデ「まえのコーデからパッと選ぶ」+被り警告・写真2択・服選びの絞り込み・骨格タグ(4.10) |
| 公式サンプル | 架空の6人が投入済み(4.9)。**2択は3日で期限切れ→数日おきに再投入スクリプトを実行** |

### ⚠ 次のセッションで最初に確認すること

**2026-08-03 の後半に入れた機能群は、まだ実機で検証されていない。**
Kazさんに触ってもらった結果を聞くところから始めること。特に:

1. クローゼットで「見本の服を入れる」を押し直すと、タグ付きの新しいデータになる
2. 2択 →「これを着る」→ 投稿 で、**服のハッシュタグが自動で乗るか**
3. 投稿カードの「**写真を保存**」が実際に端末へ保存されるか(CORS設定済みだが実機未確認)
4. `/tag/○○` が開くか。**Firestoreの複合インデックスが必要**で、
   初回はコンソールに出る作成リンクを踏む必要がある(踏むまで結果が0件になる)
5. 通知(ホーム右上)が届くか
6. **DMの既読・送信時刻・写真送信(2026-08-04 追加)**: 相手が開くと自分の最新メッセージに
   「既読」が付くか / 一覧の未読ドットが開くと消えるか / 写真が送れて表示されるか
7. **AI合成の先行実行(2026-08-04 追加)**: 2択作成の「仕上げ」で顔を選ぶと
   「AI合成を先に進めています…」→「AI合成できました」になるか / 投稿直後の2択に
   合成画像が最初から出るか / 同じ組み合わせで作り直すと合成が一瞬で終わるか(キャッシュ)
8. **値段(2026-08-04 追加)**: 服の登録・編集で値段+「公開する」を保存できるか /
   2択詳細・投稿詳細に「このコーデの値段を見る」が出るか / 無料アカウントで2コーデ目が
   ロックされるか(自分の投稿はいつでも見られる)
9. **クローゼットの新UI(2026-08-04 追加)**: カテゴリタブ(ワードローブ直上)を押してから
   左右にスワイプすると服がレール上を滑らかに流れるか / 中央の服が拡大され、下のカードを
   タップするとそこまでスライドするか / カードの星でお気に入りにでき、一覧の先頭に浮くか
   (2択作成の一覧でも先頭に来る)
10. **投票理由スタンプ(2026-08-04 追加)**: 投票後に「どうしてこっち?」が出るか /
    投稿主から理由の集計(🎨 色がすき など)が見えるか
11. **2択へのコメント(2026-08-04 追加)**: 2択詳細の下部にコメント欄が出て、
    書き込み・リアルタイム反映されるか
12. **公式サンプル6人(2026-08-04 投入)**: フィードと「今日の2択」にサンプル投稿が
    並ぶか / 投票・値段・理由スタンプ・コメントのお手本になっているか /
    2択詳細右上の「共有」ボタンでリンクを送れるか
13. **要望リスト対応(2026-08-04 第3弾)**: 2択作成に「まえのコーデからパッと選ぶ」が
    出るか(⚠◯日前に着た付き) / 「着る」確定後にコーデ保存→クローゼットの
    「コーデ」タブに並ぶか / 全身写真で2択を作れるか / 服選びの絞り込み(季節・
    ジャンル・色)が効くか / 登録でメンズ・レディースと骨格を付けられるか /
    共有相手のグループ保存→ワンタップ適用ができるか / 天気オン+投稿で
    ホームの「🌡今日の気温に近い」フィルタが機能するか
15. **第5弾(2026-08-05)★いま一番見てほしいところ**:
    - **ホームと「さがす」に投稿が並ぶか**(索引が無くて公開タイムラインが
      サーバー側で失敗していた。直したので、ここが直っていれば体感が大きく変わる)
    - **2択一覧に「あなたの2択」が出るか**(同じ理由で失敗していた)
    - **2択一覧で他人・公式の2択に服の写真が出るか**(以前は「アイテム未選択」だった)
    - **フォローが5人以下のとき**、ホームの上と2択一覧の上に
      「公式アカウントのコーデ/2択(プレビュー)」が**横に自動で流れる**か。
      指を置くと止まり、その場でフォローできるか。フォローが6人以上になると消える
    - **2回目以降がはっきり速くなったか**(画像がキャッシュされるようになった。
      クローゼット・ホームを開き直したときの差が一番分かりやすい)
    - **投稿が速くなったか**(写真を選んだ時点で圧縮を始めるので、「投稿する」を
      押してからの待ちが短いはず)
    - 「保存した投稿」の一覧が出るか(索引が無く、一度も引けていなかった)
    - **クローゼットの上の「メンズ / ウィメンズ / すべて表示」**: 既定がプロフィールの
      性別に合っているか / 選び直すとその一式だけになるか / **リロードや画面移動を
      またいでも選択が残るか** / もう一度戻せるか
14. **第4弾(2026-08-04 夕)**: フィードに公式サンプルの**着用画(人物イラスト)**が
    6枚並ぶか / プロフィール編集で**パーソナルカラーのメイン+サブ**を選べて、
    プロフィールに「サブ ◯◯」タグが出るか / **2回目以降の起動が速くなったか**
    (キャッシュから即描画→裏で最新に置き換わる) / **⚠最重要:
    フォローだけ(相互でない)の相手の「フォロワーだけ」投稿と2択がフィード・
    2択一覧に出るか**(per-owner クエリがルールに弾かれていないか。弾かれても
    画面は壊れず、単にその人の分が出ないだけの作りにしてある) /
    フォローだけの相手の2択に投票できるか / 相互フォローでない相手のプロフィールで
    DMボタンが消えているか / 相互フォローの相手とはDMできるか

### Kazさんの操作が必要に残っていること

1. 上記の実機確認(**1〜5は2026-08-03分、6〜13は2026-08-04分**。どれも未検証)。
2. **冒頭の未回答質問4つへの回答**(写真2択の有料化 / サンプルへの気温焼き込み / 地域フィルタ / デザイン素案)。
3. **Discordプラグインの許可リスト復旧**(`/discord:access`)。
4. **Stripe本番モードへの切り替え**。本人確認・口座登録の審査が要る。
   商品/価格/Webhookはテストと本番で完全に別データなので、**本番モードで作り直し**が必要。
   `sk_live_` は一度しか表示されないので、その場で控えること。

### Kazさんが示している方向性(ぶれさせないこと)

**Instagram / BeReal / TikTok のように若者層に支持される規模を目指す。**
将来はアイテムタグからブランドページへ飛ばしてアフィリエイト収益、公式アカウントや
モデルのスカウトも視野に入れている。**「Instagramの劣化版」になることを避ける**のが方針で、
このアプリにしかない軸(2択・骨格/パーソナルカラーによる推薦・クローゼット)を強めること。

なお **2026-08-03 のセッションでは「いちいち確認せず、できるものをどんどん入れてよい」**
という許可が出ている。ただし後戻りできないもの(下記)は例外として扱ってきた。

---

## 1. このアプリの目的(ぶれさせないこと)

**朝の服選びの時間を短縮する**のが主目的。2択を作って友達に選んでもらい、選ばれた方を着る、という流れが中核。
SNS機能(公開投稿・フォロー・DM)は後から足したもので、主目的を邪魔しないように配置している。

- ターゲット: 主に大学生くらいの女性。10代〜30代が使える水準。
- 参考にしたUI: Instagram / BeReal / WEAR。
- 2択を作れるのは **1日1回**(朝に決め切る運用に寄せるため)。

---

## 2. 今回のセッションで直したバグ(同じ轍を踏まないために)

すべて「一見動いているように見えるが、実は一度も成功していなかった」類のもの。
**共通する教訓: 症状から推測せず、実データ・ログ・git履歴を先に見る。**

### ①' AI合成は **2026-08-05 時点でもまだ一度も成功していなかった**(さらに原因2つ)

下の①(リージョン不一致・APIキー)を直した後も、実データでは
**全2択の候補が `composeStatus: "pending"`、`composedCache` は failed 1件だけ**だった。
Cloud Functions のログを読んで原因が2つ見つかった。**どちらもGeminiとも課金とも無関係。**

1. **見本の服の画像URLが相対パス。**Firestore には `/seed/women/xxx.png` の形で
   入っている。ブラウザは表示できるが、**サーバーの `fetch` は絶対URLしか受け付けず**
   `TypeError: Failed to parse URL` で即死していた。実データで closetItems 77件が
   **すべて**この形式 → 事実上あらゆるコーデで合成が失敗していた。
   → `functions/src/index.ts` の `absoluteImageUrl()` で公開サイトのオリジンを補う。
   **`public/` 配下の画像をサーバーから読む経路を作るときは必ずこれを通すこと。**
2. **`composeOutfitImage` の Cloud Run invoker 権限が空だった。**呼び出しが
   「The request was not authenticated」で弾かれ、**関数本体に到達していなかった**
   (`precomposeOutfit` には付いていた)。callable は Firebase SDK が独自に認証情報を
   運ぶので Cloud Run からは常に未認証に見える。invoker は public にして、
   **関数の中で `request.auth` を確認する**のが標準構成。
   → `onCall` のオプションに `invoker: "public"` を明示した。ただし
   **既存関数の更新では IAM に反映されない**ので、初回だけ手で付ける必要がある:
   ```
   gcloud run services add-iam-policy-binding composeoutfitimage \
     --project=my-clothes-46c81 --region=us-central1 \
     --member=allUsers --role=roles/run.invoker
   ```

なお **AI合成は「無料」機能**(4章の線引き)なので、**プレミアムにしても合成は変わらない**。
「課金したのにAIが使えない」の答えは「AI合成は元から無料機能で、上の2つのバグで
壊れていた」だった。課金自体は成功している(`plan: premium`)。

実際のURLとキーで Gemini まで通し、合成画像が返ることを確認済み
(`scratchpad/test_compose.py` の手順。1枚約12円かかる)。

### ① AI合成が一度も動いていなかった(原因2つ)

- **リージョン不一致**: クライアントが `asia-northeast1`、関数が `us-central1`。存在しないエンドポイントを叩いていた。
  → 両方を `us-central1` に統一。`src/lib/functions.ts` の `CALLABLE_REGION` と
  `functions/src/index.ts` の `FUNCTION_REGION` にコメントで「必ず一致させること」と明記済み。
- **APIキーが壊れていた**: Secret Manager の値が32文字・`AIza`始まりでない・空白混入で `API_KEY_INVALID`。
  → 専用キーを新規発行して登録(version 3)。

なお当初これを「一時的な問題」と誤って説明した。真因は `firebase.json` の `ignore` に `lib` が入っていたことで、
それは別セッションで既に修正済みだった。**再実行で直ったときこそ「なぜ直ったか」を確認すること。**

### ② 画面下のボタンが全部押せなかった

`BottomNav` が `fixed bottom-0 z-40`、各画面の操作バーが `bottom-0 z-30` で、**完全に裏に隠れていた**。
投稿ボタン・DMの入力欄・コメント欄がすべて機能不全。
→ `ActionBar` コンポーネント(`src/components/ui.tsx`)に集約し、`--nav-h` 分だけ持ち上げるようにした。
**画面下に固定要素を足すときは必ず `ActionBar` を使うか、同じ計算で持ち上げること。**

### ③' Stripe関数が「Firebase app が存在しない」でデプロイできなかった(2026-08-03)

`billing.ts` がトップレベルで `getFirestore()` を呼んでいた。`index.ts` は
`export { ... } from "./billing.js"` で再輸出しており、**ESMでは再輸出もimportと同様に巻き上げられるため、
`index.ts` 本体の `initializeApp()` より先に `billing.ts` のトップレベルが評価される**。
結果 `The default Firebase app does not exist` でソース解析ごと失敗していた。
→ Firestoreの取得を関数実行時まで遅らせた(`billing.ts` の `db()`)。

**新しいモジュールを作って index.ts から再輸出するときは、トップレベルで Firebase のサービスを掴まないこと。**

### ③ 全身写真の投稿が必ず失敗していた

`storage.rules` に `styles/` のルールが無く、Firebaseは未定義パスを既定で拒否する。
→ `styles/` と `avatars/` を追加。

### ④ DMが一度も使えなかった(ルールの落とし穴)

`chatThreads` が本番で0件だった。原因は **Firestoreでは存在しないドキュメントの `get` で `resource` が `null` になる**こと。
`me() in resource.data.memberUids` を無条件に書いていたため評価エラー→権限拒否となり、
「スレッドの存在確認」で必ず落ちて作成に到達できなかった。
→ `get` のみ `resource == null` を許可、`list` はメンバー限定のまま。`stylePosts` / `outfitPosts` も同じ形だったので同様に修正。

**このパターンは再発しやすい。新しいコレクションのルールを書くときは「存在しないドキュメントを読む経路があるか」を必ず確認すること。**

---

## 3. 実装済みの機能

### 中核(2択)
- 決め方を2つから選択: **「上から順に選ぶ」**(アウター→トップス→ボトムス→靴。次に選ぶ場所に「←今ここ」表示)/ **「主役から選ぶ」**。
- 2択を作成 → 友達が投票 → 「こっちを着る」で確定 → 着用回数を加算しカレンダーに記録 → 全身写真投稿へ導線。
- **友達ゼロでも作成・保存できる。**
- 1日1回制限(`hasCreatedOutfitToday`)。**クライアント側チェックのみ**。ルールでは件数を数えられないため、
  厳密にするならCloud Functions経由の作成に変える必要がある。
- **投票理由スタンプ(2026-08-04 追加)**: 投票後に「色がすき/気温に合ってる/予定に合ってる/
  なんか良い」をワンタップで添えられる(任意)。`Vote.reason`。集計は候補の下に
  「🎨 色がすき ×2」のように出る。コメントより軽い理由づけで、投稿主が納得して
  決められるようにするもの(=決める時間の短縮)。ルール変更は不要だった
  (votes の update は create と同条件で既に許可されていた)。
- **2択へのコメント(2026-08-04 追加。Kazさん依頼)**: `outfitPosts/{id}/comments`。
  全身写真の投稿と同じ `PostComment` 型・同じルールの形。**commentCount は持たない**
  (outfitPosts の update ルールが他人のカウンタ加算を許しておらず、2択は24時間で
  消えるので非正規化は不要と判断)。通知は castVote と同じく postId=null で送る
  (通知の postId は /post/ への導線専用のため。2択に飛ばす導線は未実装)。

### 全身コーデ表示(`LookFigure`。AIを使わない。2026-08-06 追加)

**AI合成が無くても全身のコーデが伝わるようにするための表示。**`OutfitCard` の
フォールバックがこれになった(以前は顔写真+服をタイル状に並べるだけだった)。
「枠にはめて並べるだけ」では着ている感じが出ないので、次の4点を作り込んである。
**ここを雑にすると一気に安っぽくなるので、いじるときは必ず目で見て確かめること。**

1. **カテゴリごとに幅をそろえ、高さは元の比率のまま。**枠に押し込む(object-fit だけ)と
   正方形に近いシャツが横に広いシャツと同じ大きさになり、大小関係が嘘になる。
   幅をそろえると**丈の長短(ロングパンツ/ショーツ)とゆとりが保たれる。**
2. **肩の線(`SHOULDER`)を固定し、そこに顔の下端をぴったり乗せる。**
   顔と襟が離れた瞬間に「切り貼り」に見える。
3. **重ね順を実際の着方に合わせる**(ボトムス→トップス→アウター)。
   ブラウスの裾がカーディガンから覗くような本物のレイヤードになる。
4. **床の影と服ごとの落ち影。**無いと全部が宙に浮く。

高さの上限を持たせてあるので、丈の長い服でも枠からはみ出さない。
想定する枠は **3:4**(すべての呼び出し元がそうなっている)。

### 見本画像の修復(2026-08-06)

`public/seed/` の26枚は **すべて `eXIf` チャンクの CRC が壊れていた**
(`broken PNG file (bad header checksum in eXIf)`)。ブラウザは CRC を見ないので
表示できてしまい、長らく気付かれていなかった。**厳密なデコーダ(Pillow など)は開けない**ので、
サーバー側で画像を触る処理を足した瞬間に壊れる地雷だった。
`scripts/clean_seed_cutouts.py --apply` で修復済み。あわせて:
- Apple の私的チャンク `caBX`(1枚最大72KB)とメタデータを除去 → **合計 約2.0MB → 612KB**
- 切り抜きに残っていた隣の服の断片を除去(`men/gray-slacks.png` の黒い影)

**見本画像を差し替えたら、このスクリプトを1回通すこと。**

### 🔑 AI合成は「利用者それぞれが自分のAPIキーを登録して使う」方式(2026-08-06 変更)

**運営(Kazさん)のGeminiキーはもう使っていない。** 以前は `GEMINI_API_KEY` を Secret Manager に
置いて全員ぶんの費用を運営が負担していたが、Kazさん指示で **BYOK(各自のキー)** に切り替えた。

- 保存先は **`userSecrets/{uid}`**。ルールで**本人しか読み書きできない**独立コレクション。
  **`users` ドキュメントには絶対に入れないこと。** users は `allow read: if isSignedIn()` なので、
  そこに置くとサインインした他人全員に他人のAPIキーが読める。
- Cloud Functions は Admin SDK でこのコレクションを読み、**呼び出した本人のキー**で Gemini を叩く
  (`requireUserGeminiKey()`)。未登録なら `failed-precondition` で「プロフィール編集画面から登録して」と返す。
- 登録・確認・削除は **プロフィール編集画面**。`verifyGeminiKey` callable が保存済みのキーで
  実際にAPIを叩いて可否を返す(**キーの中身はクライアントに返さない**)。
- クライアントは `useAuth().hasGeminiKey` を見て、未登録の人には合成を発火させない
  (叩いても関数側で弾かれるだけなので無駄)。
- **1日の合成回数上限(旧 `DAILY_COMPOSE_LIMIT` / `composeQuota`)は撤去した。**
  あれは運営の請求額を抑えるための制限で、各自が自分のキーで払う今は縛る理由がない。
  無駄打ちの防止はキャッシュ(`composedCache`)が担う。
- **AI合成は無料機能**(プレミアムでもない)。費用は各自の Google アカウントに請求される。

未登録でもアプリは全部使える。その場合は `LookFigure`(AIなしの全身コーデ表示)になる。

### AI合成
- `composeOutfitImage`(Cloud Functions, us-central1)が顔写真+服の写真をGeminiに渡して1枚に合成。
- モデル: `gemini-3.1-flash-image`。`functions/.env` の `GEMINI_IMAGE_MODEL` で変更可。
- **合成が無い/失敗/処理中でも `OutfitCard` が顔写真+服をボード状に必ず表示する。**
  「顔を撮ったのに映らない」という指摘への対処であり、課金が切れても破綻しない設計。

#### AI合成の入/切(2026-08-06 追加)

**`NEXT_PUBLIC_AI_COMPOSE_ENABLED=false` を Vercel の環境変数に入れるだけで止まる**
(コード変更もデプロイも不要。既定は入)。止めても画面は壊れず、`LookFigure` に落ちる。

止められるようにした理由: **先行合成は「仕上げ」ステップに入っただけで発火する**ので、
投稿しなくても2択1回あたり約24円(1枚約12円×2)が課金される。実測トークンは
出力1,270 → $60/1M で $0.076/枚。使わない方針にするなら必ず切ること。

#### 合成の高速化とコスト対策(2026-08-04 追加。Kazさんの「合成待ちを早めて」への対応)
- **先行合成 `precomposeOutfit`**: 2択作成の「仕上げ」ステップに入った時点でクライアントが
  裏で合成を開始する(`create/page.tsx` の precompose effect)。本人が気分・共有相手を
  入力している時間で合成が終わるため、投稿した瞬間から合成画像が出ることが多くなる。
  発火は仕上げステップ限定——服選びの途中で発火させると、選び直しのたびに無駄な合成が
  走ってトークンを浪費するため。
- **合成キャッシュ `composedCache/{sha256キー}`**: キーは「モデル名+uid+顔URL+服の画像URL
  (ソート済み)+プロフィール指示文」。同じ組み合わせは Gemini を呼ばず即返す。
  取り消して作り直した場合や、同じコーデをまた着る日はトークン消費ゼロ。
  pending ロック付きで、先行合成と投稿後合成が同時に走っても**二重にGeminiを呼ばない**
  (後から来た方は待って結果をもらう)。**新しい合成経路を作るときは必ず `composeCached()`
  を通すこと。** 直接 `composeWithGemini()` を呼ぶとキャッシュも回数制限も素通りする。
- **1日の回数上限 `composeQuota/{uid}`**: 新規のGemini呼び出しは1人20回/日(JST基準)。
  先行合成は投稿前でも呼べるため、「2択は1日1回」だけでは合成回数を縛れない。
  1枚約10円なので、この上限が最大コスト(約200円/人/日)の天井になる。
  キャッシュヒットは消費しない。上限到達時は resource-exhausted を返し、投稿自体は
  従来どおり成立する(ボード表示に落ちる)。
- **2択詳細(`vote/[postId]`)は `watchOutfitPost` で購読**に変更。以前は1回のgetで、
  合成が終わっても画面に反映されずリロードが必要だった。一覧(`/vote`)は元から購読。
- **顔写真のアップロードは `uploadLiveCapture()` で使い回す。**先行合成と投稿時で別URLに
  上げ直すとキャッシュキーが変わってヒットしなくなるため(create/page.tsx 内のコメント参照)。

### 費用の目安(2026-08時点、公式料金で確認済み)
- `gemini-3.1-flash-image`: 出力 $60/1M tokens、1枚=1,120 tokens → **1枚約$0.067(約10円)**
- 2択1回で2枚生成 → **約20円**。2,000円で**約100回**。
- 安価版 `gemini-3.1-flash-lite-image` は半額(1枚約5円)。高品質 `gemini-3-pro-image` は約20円/枚。
- 入力(画像6枚程度)は1回0.5円未満で無視できる。
- 2026-08-04 のキャッシュ導入で、同じ組み合わせの再合成は0円になった。上限は20回/日/人。

### 将来のトークンコスト・Gemini依存への構え(2026-08-04 に方針整理)
Kazさんの問題意識:「たくさん合成するとトークンがかかる。ずっとGeminiに頼り続けるのも微妙」。
実装済みの対策と、規模が伸びたときの選択肢を利用順に並べておく。

1. **済: キャッシュ+日次上限**(上記)。コストの天井が計算できるようになった。
2. **すぐできる: モデルの引き下げ。**`functions/.env` の `GEMINI_IMAGE_MODEL` を
   `gemini-3.1-flash-lite-image` にすれば半額(画質は要確認)。コード変更不要、デプロイのみ。
   「無料ユーザーはlite・プレミアムは標準」の出し分けは composeCached にモデル引数を
   足せば作れる(キーにモデル名が入っているので混ざらない)。
3. **中期: 合成せずに成立する見せ方を磨く。**`OutfitCard` のボード表示(顔+服を並べる)は
   すでに全フォールバックとして機能している。ここをコラージュ風に整える(服の背景除去、
   紙面風レイアウト)だけなら**APIコストゼロ**で、AI合成を「プレミアムのおまけ」に
   格下げする選択肢も取れる。背景除去はクライアントで動く軽量モデル(例: @imgly/background-removal)がある。
4. **長期: 自前ホストの試着モデル。**IDM-VTON / OOTDiffusion 等のオープンモデルをGPUサーバーで
   動かせば従量課金から固定費に変わる。ただし GPU 運用(コールドスタート・保守)が重く、
   **数人規模では確実に割に合わない**。月の合成数が数千枚を超えてから検討する話。
5. **アイデア止まり: Gemini Batch API は半額だが非同期で数分〜数時間かかる。**
   「朝すぐ見たい」という本アプリの用途と合わないので不採用とした。

### クローゼット
- ブランド / サイズ / 色 / ジャンル(10種) / 季節 / メモ / **値段** を登録。カテゴリー7種(ワンピース・バッグ含む)。
- ジャンル・季節・カテゴリーで絞り込み。**季節タグ未設定の服は通年扱いで常に表示**(登録直後に消えると混乱するため)。
- **ワードローブ表示(2026-08-04 に全面変更。Kazさん指定の参考画像+同日の追加指示)**:
  `src/components/WardrobeCloset.tsx` の `WardrobeCarousel`。木製ワードローブ(扉開き)の
  中で服が**横スワイプでレールの上を流れる**カルーセル。中央の服が選択中(拡大表示)で、
  左右に前後の候補が実物の写真で見える。CSS scroll-snap 準拠(JSアニメーションではなく
  ネイティブの慣性スクロールに乗せる)。中央判定は scroll+rAF で最近傍を取る。
  **カテゴリタブはワードローブの直上**に移してある(タブを押してから左右に流す、という
  Kazさん指定の操作順)。下の白カードグリッドは**従来デザインのまま維持**(Kazさん指定。
  タップすると上のカルーセルがその服まで滑らかにスライドする)。中央の服タップ or
  「詳細・編集」で編集シート。木の色はイラスト用の配色で、アクセント青とは独立。
  旧 `HangerRail` は**コーデ作成画面ではまだ現役**なので消さないこと。
- **メンズ / ウィメンズ / すべて表示の切り替え(2026-08-05 追加。Kazさん依頼)**:
  クローゼットの**いちばん上**に置いてある。選んだ一式の服だけが出て、末尾の
  「すべて表示」で両方出る。
  - 既定は**プロフィールの「主に使う服」**(`primaryWardrobe`)。この画面で選び直すと
    そちらが優先され、**端末に残る**(`localStorage` の `mc.closetWardrobe.<uid>`)。
    リロードしても他の画面から戻っても引き継がれ、いつでも戻せる
  - サーバーに持たせていないのは、`primaryWardrobe` が2択作成の既定値も兼ねる
    別物だから。ここでの「見せ方の切り替え」と混ぜると、クローゼットを覗いただけで
    コーデ提案の対象まで変わってしまう
  - **メンズ/ウィメンズを指定していない服は、どちらを選んでも残す。**指定は任意項目で、
    付け忘れた自分の服が消えるほうが困る(2択作成の出し分けと同じ考え方)
  - 件数・カテゴリタブ・「眠っている服」の数も、表示中の一式で数え直している。
    **ここを `items` のままにすると「メンズ (4)」と中身が食い違う**
  - 見本の服を入れ直したときは、入れた一式に表示を合わせる(`applySeedResult`)。
    でないと「ウィメンズ表示のままメンズの見本を入れて1着も出ない」が起きる
- **お気に入り(星)**: カード右上の星で付け外し。クローゼットと2択作成の一覧で
  **先頭に浮く**(よく着る服ほど早く手に取れる=選ぶ時間の短縮)。ただし
  「眠っている順」ソートでは優遇しない(掘り起こしという目的に反するため)。
- 編集・削除、着用回数の記録。

### 値段(2026-08-04 追加。Kazさん依頼)
- `ClosetItem.price`(円)+ `pricePublic`(公開するか。既定は非公開)。登録・編集の両方に入力欄。
- 表示は `src/components/PricePanel.tsx` に集約。2択詳細(候補A/B別+合計)と
  投稿詳細(タグ付けした服+合計)に出る。
- **無料は1日1コーデまで、プレミアムは無制限**(Kaz指定)。閲覧記録は
  `UserProfile.priceViews {date, postIds}` で、同じ投稿の2回目は消費しない。
  自分の投稿はいつでも見られる(非公開の値段も「非公開」の印つきで出る)。
- ⚠ closetItems はサインイン済みなら誰でも読めるので、**この制限は画面側の制御**であり
  暗号的な秘匿ではない(1日1回制限などと同じ扱い。数人規模では十分という判断)。
- 勧誘はパネル内の1画面だけ(執拗な課金勧誘をしない方針を維持)。

### 投稿・SNS
- **全身写真の投稿**: 写真をタップした位置にアイテムタグを置ける(WEAR方式)。公開範囲は「みんな」/「友達だけ」。
  **友達ゼロでも公開できる。** 2択で確定した直後に投稿すると、その服が自動でタグの初期値に入る。
- 公開タイムライン、ダブルタップいいね、コメント、フォロー/フォロワー、ユーザー検索(@ハンドル前方一致)。
- **DM**(E2E暗号化なし、指示どおり)。フォローしている相手なら誰とでも開始可能。
  - **既読・送信時刻・写真送信は 2026-08-04 に追加**(Kazさんの明示依頼。写真は以前の
    「当面テキストのみ」指示をKazさん自身が上書きしたもの)。
  - 既読は `chatThreads.lastReadAt: {uid: 時刻}` との比較で判定(メッセージ個別のフラグは持たない)。
    ルールで **lastReadAt は自分のキーしか書けない**(`readMarksOnlyMine()`)。
    「既読」ラベルは自分の既読済み最新1件だけに表示、一覧は未読ドット+太字。
  - 写真は `compressImage` → Storage `chat/{uid}/`(storage.rules に追加済み)。
    `capture` 属性は付けない(付けると保存済み写真を選べなくなる。アイコン変更と同じ理由)。
    テキスト無しで写真だけ送ると一覧の lastMessage は「写真を送りました」になる。

### プロフィール
- @ハンドル、自己紹介、身長、骨格タイプ、パーソナルカラー(**メイン+サブの2つまで**。
  2026-08-04 第4弾。`UserProfile.personalColorSub`、"unknown"=サブなし。おすすめユーザーの
  カラー一致判定はメイン・サブどれかが重なればヒット)、サイズ3種、好きなジャンル。
- **お気に入りコーデを3つまで固定表示**(自分・他人の両方の画面。他人からは非公開投稿を除外)。
- **アイコン変更はプロフィール編集画面**にある(プロフィール画面ではない)。
  `<input accept="image/*">` から `capture` を**意図的に外している**。付けるとカメラが直接起動し、
  端末保存済みの写真から選べなくなるため。

### カレンダー
- 月グリッドに その日着たコーデのサムネイル。全身写真優先、無ければ確定した2択の服の写真。

### つながりの定義(2026-08-04 第4弾で再設計。旧「友達」)
**公開範囲・投票の基準は「フォロー」、DMの基準だけ「相互フォロー」。**

- 投稿の visibility `'friends'` は表示上**「フォロワーだけ」**に改名(データ値は互換のため
  `'friends'` のまま)。**投稿主をフォローしていれば読める**(firestore.rules の `iFollow()`)。
- 2択も同様: 公開/共有相手に加えて**フォロワーなら閲覧・投票できる**。
- フィードと2択一覧は「公開分」+「フォロー中の人の分(per-owner クエリ、先頭30人まで)」を
  マージ。per-owner クエリはルール評価に弾かれた場合その相手の分だけ黙って落ちる設計
  (エラーコールバックで握る)。**実機で必ず確認すること(上の確認項目14)。**
- **DMは相互フォロー限定**。UI(ボタン非表示・送る相手一覧)とルール
  (chatThreads の create で相手の friendUids に自分が入っていることを要求)の両方で強制。
  既存スレッドの継続は縛らない。
- `friendUids` は相互フォロー状態の非正規化キャッシュとして残っている(DM判定・
  招待コードで使用)。UI上の「友達」表記は「相互フォロー」「フォロワー」等に置き換え済み。
- 招待コードは従来どおり: フォローを張ったうえで相互(friendUids)にする=DMも即可能。

---

## 4. 課金機能(Stripe)

### 実装済み
| ファイル | 役割 |
|---|---|
| `functions/src/billing.ts` | `createCheckoutSession` / `createBillingPortalSession` / `stripeWebhook` |
| `src/app/upgrade/page.tsx` | プラン説明・申し込み・解約 |
| `src/app/legal/tokushoho/page.tsx` | 特商法表記(**未記入の雛形**) |
| `BILLING_SETUP.md` | Stripe側の設定手順(全ステップ) |

### 設計の要点
- **`plan` はクライアントから書き換え禁止**(`firestore.rules` で `plan` を含む更新を拒否)。
  Admin SDK(= Webhook)経由でしか書けない。これがないと開発者ツールで1行書き換えるだけで有料機能が開く。
- Webhookは**生ボディで署名検証**してから信用する。ハンドラ失敗時は500を返してStripeに再送させる。
- カード情報はアプリを一切通らない(Stripe Checkout)。
- 解約導線は勧誘と同じ画面に置き、隠していない。

### 有料/無料の線引き(Kaz指示)
- **有料(プレミアム)**: ①おまかせ提案(自動でコーデを組む機能)、②今日の2択の**無制限**の取り消し、
  ③**他の人のコーデの値段の無制限閲覧**(2026-08-04 追加)。
- **無料**: それ以外すべて。**AI画像生成は無料**。2択、クローゼット、投稿、SNS、DM、カレンダー。
  今日の2択の取り消しも **1日1回まで**は無料でできる(`FREE_UNDO_PER_DAY`)。
  値段の閲覧も **1日1コーデまで**は無料(`FREE_PRICE_VIEWS_PER_DAY`)。

### 2択の取り消しの作り(2026-08-03 追加)
物理削除ではなく `OutfitPost.deletedAt` に印を付ける**論理削除**。ドキュメントごと消すと
「今日なん回取り消したか」を数える対象が無くなり、無料プランでも無制限に作り直せてしまうため。
- `firestore.rules` の update は `['decidedCandidateIndex', 'deletedAt']` のみ許可(デプロイ済み)
- 一覧系(`watchFeedPosts` / `watchMyPosts` / `listMyOutfitPosts`)は `deletedAt` を除外する。
  **生の一覧が必要なのは回数を数えるときだけ**で、それは `listMyOutfitPostsRaw`(非公開)を使う
- 判定はクライアント側のみ。厳密にするならCloud Functions経由に変える必要があるのは
  1日1回制限と同じ事情

### 勧誘方針(厳守)
**執拗な課金勧誘は絶対にしない。** 案内はロックされた項目をタップしたときの1画面だけ。
常時表示バナー、再訪の催促、期間限定を煽る表示、閉じにくいダイアログは入れない。

### 設定状況(2026-08-03 時点・すべて**テストモード**)
- 価格: **月額330円(税込)**。Stripeには `330` で登録(Stripe Taxは未使用なので登録額がそのまま請求される)
- price ID は `functions/.env` の `STRIPE_PRICE_ID`(このファイルは `.gitignore` の `.env*` によりコミットされない。
  **別マシンからデプロイする場合は作り直しが必要**)
- `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` ともSecret Managerに登録済み
- Webhook URL: `https://us-central1-my-clothes-46c81.cloudfunctions.net/stripeWebhook`
  (デプロイログには `*.run.app` の別名も出るが、Stripeにはcloudfunctions.net版を登録してある)
- 購読イベント4種、カスタマーポータル(解約許可)ともに設定済み
- Vercel環境変数 `NEXT_PUBLIC_BILLING_ENABLED=true` 設定済み

**シークレットを更新したら必ず再デプロイすること。**関数はデプロイ時点のバージョンを掴むので、
Secret Managerを書き換えただけでは反映されない。

### 未完了
- 実機でのテスト決済(`4242 4242 4242 4242`)による検証。確認項目は3つ:
  決済後に `plan` が premium になるか / おまかせ提案が解禁されるか / 解約で `free` に戻るか
- 本番モードへの移行(商品・価格・Webhookをlive側で作り直し、`sk_live_` と新しい `whsec_` を登録)
- `firebase-functions` がメジャーバージョン遅れ。デプロイ時に警告が出る。破壊的変更を含むため保留中

---

## 4.4 配色(2026-08-03 に青系へ変更)

アクセントは **青 `#3b6bff`**。以前はピンク `#ff3b6b` だったが、
**「Instagram に近すぎる」というKazさんの指摘で青系に振った。**
構造・余白・角丸は一切変えておらず、変えたのは `globals.css` の色トークンのみ
(＋ `layout.tsx` の themeColor と `manifest.ts`)。地色や罫線もわずかに寒色へ寄せてある。

## 4.5 デザインについて(2026-08-03 に一度刷新し、差し戻した経緯)

`powerpoint_design_textbook/fashion_cheeful.pptx` を参照して、ネオンライム(#EBFC34)+
ニアブラック+ライトグレーのエディトリアル調に全面刷新したことがある(コミット `1868d13`)。
**Kazさんの判断で全て元に戻した**(`白基調 + ピンク #ff3b6b` の現行デザイン)。

- **現行デザインが正。**上記のpptx路線を再提案しないこと。好みに合わないと確認済み。
- 差し戻し後も、初期クローゼットの写真とオンボーディングの選択機能だけは残してある。
- 当時の実装を見たい場合は `git show 1868d13` で参照できる。

## 4.6 初期クローゼット(実物の写真)

`public/seed/men/`(12点)と `public/seed/women/`(14点)。定義は `src/data/seedClosetItems.ts`。
オンボーディングで「レディース / メンズ / 両方」を選ばせ、選んだ側だけを投入する。

- **元画像の解像度が低い**(最大でも 138×233px)。表示は現行デザインどおり `object-cover` で、
  商品撮影の写真としては裾や袖が切れる。`object-contain` にすると全体が入り引き伸ばしも減るが、
  デザイン差し戻しの際に現行の見え方へ揃えた。**同じファイル名で高解像度版を上書きすれば
  コード変更なしで差し替わる。**
### 「主に使う服」による出し分け(2026-08-03 追加)
`UserProfile.primaryWardrobe`(`"men" | "women"`)を設定画面(プロフィール編集)で選ぶ。
**2択を作る画面では、既定でこちら側の見本しか出さない。**メンズとウィメンズを両方入れると
一覧が倍になって選びにくいため。おまかせ提案も同じ一覧に従う。

- 一覧の末尾に「〇〇の服も見る」を置いてあり、その場で反対側も出せる
- **自分で登録した服は常に出す。**`ClosetItem.wardrobe` が付くのは見本だけ
- `wardrobe` が無い古い見本は、画像パス(`/seed/men/` `/seed/women/`)から補う
  (`wardrobeOfItem()`)。移行作業を不要にするため
- `primaryWardrobe` が未設定なら出し分けをしない(全部出す)

- **レディースに靴、メンズにアウターとワンピースが無い**(元の写真に個別の切り出しが
  存在しなかった)。「上から順に選ぶ」フローでそのカテゴリーが空になる。写真を足すなら
  `cloth_image/` に置いて同じ手順で追加すること。
- 旧イラスト(SVG 10点)は `Delete_candidate/my_clothes_seed_svg_20260803_185455/` へ退避済み。
  デザインは戻したが、服のイラストは使わない方針は継続している。

## 4.7 SNSとしての拡張(2026-08-03 追加)

Kazさんの方針: Instagram / BeReal / TikTok のように若者層に支持される規模を目指す。
将来はアイテムタグからブランドのページへ飛ばしてアフィリエイト収益、公式アカウントや
モデルのスカウトも視野。その**土台**として次を入れてある。

### 天気連動(`src/lib/weather.ts` / `WeatherBar`)
- Open-Meteo(天気)と BigDataCloud(逆ジオコーディング)。**どちらもAPIキー不要**。
  鍵が要るサービスを選ぶとクライアントに置けず、Cloud Functions 経由になって
  無料枠と速度を圧迫するため、この2つを選んでいる
- 位置情報は**本人が押したときだけ**取得。座標は小数第1位に丸めて送る(約11km四方)
- 気温は `suggestOutfit({ maxTemp })` に渡り、寒い日はアウターを加点、真夏は減点する
- 同意と当日ぶんのキャッシュは localStorage

### Gemini プロンプトの個別化(`functions/src/index.ts`)
`buildPersonalHint()` が、身長・骨格タイプ・パーソナルカラー・サイズを合成指示に混ぜる。
**「似合う服に差し替える」ことはしない。**本人が選んだ服をそのまま着せたうえで、
シルエットの出方だけ寄せる。自分の姿を確認する機能なので、事実と違う姿を返すと無意味になる。
プロフィールが読めなくても合成は続行する(個別化は"あると良い"程度の位置づけ)。

### アフィリエイトの土台(`ItemTag.url` / `buildOutboundUrl`)
タグに購入リンクを持たせられる。**外部リンクは必ず `buildOutboundUrl()` を通す**こと。
計測パラメータを足す場所を1箇所に集めてあり、直接 href に入れると後から手が付けられなくなる。
`rel="nofollow sponsored"` 付き。危険なスキームはこの関数で弾いている。

### 公式アカウント(`UserProfile.official`)
`plan` と同じく **firestore.rules で本人にも書けないようにしてある**。
自己申告で付けられるバッジは意味が無いため、運営が Admin SDK から付ける前提。
表示は `VerifiedBadge`(自分と他人のプロフィール画面)。

### 場所(`StylePost.placeName`)
**市区町村までしか保存しない。座標は持たない。**若い利用者が多い想定で、
自宅が特定できる粒度を最初から持たないことにしている。天気の同意とは別扱い
(天気は自分だけが見る、場所は他人から見えるため)。

### 投稿の編集
キャプション・公開範囲・場所・ジャンル・季節・タグのリンクを後から直せる。
**写真だけは差し替えられない。**いいねが付いた後に中身をすり替えられると、
反応の意味が変わってしまうため(ルールでも許可キーに含めていない)。

## 4.8 SNSの土台(2026-08-03 第2弾)

### ハッシュタグ
**服そのものに `hashtags` を持たせ、投稿でその服をタグ付けすると自動で引き継ぐ**のが要。
着るたびに打ち直させないための設計で、見本の服26点には最初から付けてある。
投稿側では「服由来 + キャプション中の #タグ + 手入力」を `mergeHashtags` で合流(上限20)。

> ⚠ **保存も検索も必ず `normalizeHashtag()` を通すこと。**「#白シャツ」と「白シャツ」が
> 別タグになると、集まるはずの投稿が分断される。`/tag/[tag]` で一覧。
> 複合インデックス(visibility + hashtags)が要るので、初回実行時にコンソールのリンクを踏む。

### ブロック・通報
- `blocks/{blocker__blocked}`。ID を固定してあるので1件取得で判定でき、クエリが要らない
- **除外はクライアント側が主。**全投稿でルール判定すると読み取り課金が件数ぶん増えるため。
  例外は DM の作成で、相手の画面に直接届くのでルールでも止めている
- `AuthProvider.hiddenUids` に「自分がブロックした人 + 自分をブロックした人」を持ち、
  全画面で同じ基準で除外する
- **ブロックしても相手に通知しない。どちら向きのブロックかも画面に出さない**
- `reports` は誰にも読めない(本人にも)。通報の有無が相手に伝わる経路を作らないため

### おすすめユーザー(`SuggestedUsers`)
骨格タイプ・パーソナルカラー・身長・好きなジャンルの近さで推薦。
**これは他のSNSには作れないこのアプリ固有の軸**なので、ここを弱めないこと。
理由を必ず添える。友達3人未満のときはフィードの投稿より先に出す。
現状は「新しめのユーザーを最大200件読んで並べ替える」暫定実装。

### 2択の公開(`OutfitPost.visibility`)
公開すると友達以外も投票できる。古い投稿には `visibility` が無いので、
ルールでは `get('visibility','friends')` で既定値を補っている。
友達向けと公開の2本を別購読して画面側で混ぜる(Firestore は OR を1クエリで書けない)。

> ⚠ **2026-08-04 に直したバグ**: votes の read ルールが「投稿主か共有された友達」の
> ままで、**公開2択は投票できるのに集計が読めなかった**(watchVotes が黙って失敗し、
> 投票後も「タップで投票」のまま)。read に「公開投稿なら誰でも」を追加して解消。
> 公開系の機能を足すときは、**書き込みだけでなく読み取りルールも公開に追従しているか**
> 確認すること。

### 2択一覧の「未投票を先頭に」(2026-08-04 追加)
`/vote` の友達の2択は、自分が**未投票のものを先頭**に並べ、投票済みには「投票済み」
バッジと「結果を見る」CTAを出す(`hasVotedOn` で votes/{自分} の存在を1件ずつ確認)。
「まだ選んであげていない友達」から片付ける=相手の待ち時間を減らすための並び。
公開2択の投稿主(友達でない人・公式サンプル)は `getUserProfile` で補完して名前を出す。

### 通知(`notifications/{uid}/items`)
**行為者が相手の受信箱に直接書く。**Cloud Functions を挟むより速く無料枠も使わない。
ルールで「自分が actor である1件」に縛ってある。送信は投げっぱなしで、
通知の失敗でいいねやフォロー自体が巻き戻らないようにしてある。
未読は件数を持たず `lastReadNotificationAt` との比較で数える(端末間でずれない)。

### 共有・写真の保存
`src/lib/share.ts` に集約。**段階的に落とす**のが方針で、共有シート → クリップボード、
保存は fetch→Blob → 共有シート → ダウンロード → 新しいタブで長押し、の順に落ちる。
別オリジンだと `download` 属性が無視されるので Blob 化が必須。
**Storage バケットに CORS を設定済み**(GET / 本番とlocalhostのみ)。ここを消すと保存が壊れる。

### クローゼットの書き出し・読み込み
JSON(`my-clothes.closet.v1`)。画像はURLで持つ(実体を詰めると数十MBで生成に失敗する)。
**読み込みは既存を消さずに追加するだけ。**読み込みで手持ちが消えるのが一番困るため。

### 保存(ブックマーク)
`stylePosts/{id}/saves/{uid}`。likes と同じ形で1人1保存を構造的に保証。
一覧は `collectionGroup` で引く。

## 4.10 要望リストへの一括対応(2026-08-04 第3弾。Kazさんの箇条書き要望)

Kazさんの要望リスト(前のコーデから選ぶ/カレンダーで被り防止/クローゼットにコーデ/
全身写真で比べる/絞り込み/メンズ・レディース登録/気温で他人の服装を見る/グループ/骨格)
に一括対応した。実装は以下。

- **保存コーデ `savedOutfits/{id}`**(本人のみ読み書き。ルール追加済み):
  名前付きの服の組み合わせ。2択で「着る」と確定→「コーデとして保存」ボタン(vote詳細)。
  クローゼットに**「アイテム / コーデ」切り替え**を追加し、コーデ側で名前変更・削除できる。
- **「まえのコーデからパッと選ぶ」**(コーデ作成のbuildステップ上部):
  保存コーデ+過去の2択で確定した組み合わせ(署名 `outfitSignature()` で重複排除)を
  横スクロールで並べ、タップで候補に流し込む。**「⚠ ◯日前に着た」表示で被りに気付ける**
  (7日以内は赤字)。「着る」確定時に保存コーデの lastWornAt も同期する。
- **写真2択 `OutfitCandidate.photoUrl`**: 服を選ぶ代わりに「いま撮った全身写真」を候補に
  できる(buildステップの破線ボックス)。AI合成はせず写真をそのまま表示(OutfitCard)。
  服も選んであれば値段・タグは通常どおり付く。**今は無料**(Kazさんは有料でも可と発言。
  有料化する場合は buildステップのボタンを isPremium で分岐するだけ)。
- **服選びの絞り込み**: buildステップに季節・ジャンル・色のチップ。色は手持ちの服の
  色欄から頻度順に最大8つ自動生成。
- **登録時のメンズ/レディース**(`ClosetItemInput.wardrobe`): 登録・編集フォームに追加。
  指定なし(null)は従来どおり常に表示。「主に使う服」の出し分けに自分の服も乗るようになった。
- **骨格タグ `ClosetItem.bodyTypes`**: 登録・編集で「合う骨格」を自分の感覚で複数選択。
  2択詳細のアイテムチップに**「◎骨格に合う」**(投稿主の骨格と一致時)、
  おまかせ提案でも +1.2 加点(`recommend.ts` の bodyType)。
- **グループで選んでもらう `UserProfile.friendGroups`**: 2択の共有相手の選択を名前付きで
  保存し、チップのワンタップで適用。✕で削除。プロフィール自己更新の範囲なのでルール変更不要。
- **気温×コーデ**: 投稿時に `StylePost.tempC`(その日の最高気温)を焼き込み
  (天気オプトイン時のみ)。ホームに**「🌡 今日の気温に近い(±3℃)」フィルタ**。
  過去の投稿には気温が無いのでフィルタに出ない(これから貯まる)。
  カードには「🌡 26℃の日」表示。**地域での絞り込みは未実装**(placeName はあるが
  母数が少ないうちは空振りが多いだけなので見送り。人が増えたら地域チップを足す)。

## 4.9 公式サンプルアカウント(2026-08-04 追加。Kazさん依頼)

ユーザーがゼロの期間でも画面が寂しくならないよう、**架空の6人**を投入してある。
女性3(あおい20カジュアル / りん24きれいめ / ひなの18ガーリー)+
男性3(ゆうと21ストリート / かい26きれいめ / そら28ナチュラル)。

- **実在しないことの明示**: 全員のプロフィールと投稿本文に「※アプリ公式のサンプル
  アカウントです。人物・ブランド(MC STUDIO)は実在しません」を明記。
  アバターはフラットイラスト(SVG)、コーデ投稿は服の「置き画ボード」で、
  **人物写真は一切使っていない**(架空だと視覚的にも分かるように)。
- **ブランドはすべて架空の「MC STUDIO」**。アイテムタグ・値段公開(pricePublic)・
  投票理由スタンプ・2択コメントのお手本を兼ねる。全員 `official: true`(認証バッジ)。
- 中身: 各自クローゼット4〜5着(見本写真を流用)+ 公開の2択1本(期限3日)+
  置き画投稿1本 + **着用画1本(2026-08-04 第4弾。Kazさん依頼「本人が着ている画を必ず1枚」)**。
  6人は相互フォローで、互いに投票・理由・コメント・いいね済み。
- **着用画はアバターと同じ配色のフラットイラスト**(人物写真を使わない方針のまま)。
  生成は `scripts/seed_demo_accounts.py` の `wearing_svg()` + `_body_*()` 群。服の色は
  クローゼットの実物写真に合わせ、アイテムタグ・値段も付けてある。見た目を確認したい
  ときは Edge のヘッドレススクリーンショットが使える(SVG→HTML→
  `msedge --headless=new --screenshot=... file:///...`。今回この方法で6枚検証済み)。
- **投入スクリプト**: `scripts/seed_demo_accounts.py`。
  `$env:GTOKEN = (gcloud auth print-access-token); python scripts/seed_demo_accounts.py`
  ドキュメントIDは全部 `demo_` 始まりの固定値なので、**再実行=同じIDへの上書き**で重複しない。
  **2択は3日で期限切れになるので、賑わいを保つには数日おきに再実行する**(createdAt が
  実行時刻で更新される)。消したいときは users / closetItems / stylePosts / outfitPosts /
  follows の demo_ 始まりを削除。画像は Storage の demo/ 配下。
- Firebase Auth のアカウントは作っていない(ログインしない前提のデータのみ)。
  デモユーザーへのDMは相手が読まないだけで送れてしまう点に注意(実害はないので放置)。

## 4.11 読み込みの速さ(2026-08-05 に全面的に手を入れた)

Kazさんの指摘:「開いてから動き出すまでが遅い」「検索や投稿に時間がかかる」
「ホーム画面の更新やおすすめアカウントに大変時間がかかる」。
原因は**1つではなく4層**あった。上から効いた順に並べる。

### ① 索引が無く、クエリがサーバー側で失敗していた(最大の原因)
冒頭の🚨を参照。`firestore.indexes.json` を追加し `firebase deploy --only firestore:indexes` 済み。
**新しいクエリ(equality + orderBy の組み合わせ、collectionGroup)を足したら、
必ずこのファイルも更新してデプロイすること。**
実データで成否を確かめる手順は6章「検証で使える手段」に足してある。

### ② 画像がブラウザにキャッシュされていなかった
Storage の既定は `Cache-Control: private, max-age=0`。`uploadImage` が
`public, max-age=31536000, immutable` を付けるようにした。ファイル名は必ずUUIDなので
中身が入れ替わることはなく、immutable で安全(アイコン変更も新しいUUIDになる)。
既存ファイルは `scripts/backfill_cache_control.py --apply` で塗り直し済み。
**新しいアップロード経路を作るときは必ず `uploadImage` を通すこと。**

### ②' collectionGroup クエリにはルールも索引も**別に**要る
`match /stylePosts/{postId}/saves/{saveUid}` のような**固定パスのルールは
collectionGroup クエリには適用されない**(Firestoreの仕様)。再帰ワイルドカード
`match /{path=**}/saves/{saveUid}` を別に書く必要がある。これが無かったので、
「保存した投稿」は索引とルールの両方で塞がれていた。2026-08-05 に両方追加済み。
**collectionGroup を使うときは、索引(fieldOverrides)とルール(`{path=**}`)を
セットで用意すること。**

### ③ 往復回数(1画面あたりのクエリ数)
- **いいね/保存**: カードごとに `hasLiked`+`hasSaved` を呼んでいた(20枚で40往復)。
  → `AuthProvider` が起動時に collectionGroup で一括取得し、全カードに配る。
  索引が使えない環境では `reactionsReady=false` になり、従来の個別確認に落ちる
- **公開タイムライン**: ホームと「さがす」が別々に購読していた。
  → `src/lib/liveStore.ts` の `subscribePublicPosts` で1本に共有。
  最後の購読者が離れても90秒は生かすので、行き来しても読み直しが起きない
- **おすすめの人**: `users` を無条件に200件読んでいた。
  → 公式(6件)+ 投稿数上位30件に。結果は10分キャッシュ。
  依存配列に `profile` オブジェクトや `Set` をそのまま入れていたため、
  中身が同じでも参照が変わるたび再取得していたのも直した(文字列キーにする)
- **フォロー一覧**: ホームと2択一覧が別々に引いていた → `AuthProvider` に集約
- **2択作成**: 同じクエリ(ownerUid==自分)を3回投げていた → `loadMyOutfitState` で1回
- **2択詳細**: 相手のクローゼットを丸ごと読んでいた → 候補に写っている服だけID指定
- **他人のプロフィール群**: 1件ずつ getDoc → `documentId()` の `in` 検索で30件ずつ

### ④ 一度に描く量
- 公開タイムラインの購読は100件 → 40件
- ホームは6枚ずつ、「さがす」は18マスずつ描き、下端が見えたら足す(IntersectionObserver)
- 一覧・グリッド用に**サムネイル(長辺480px)**を投稿時に作って上げる(`StylePost.thumbUrl`)。
  **参照側は必ず `thumbSrc(post)` を使うこと**(これを足す前の投稿には無いので本体に落ちる)
- 投稿の圧縮は「投稿する」を押してからではなく、**写真を選んだ瞬間**に始める
  (`prepareUpload`。本人がキャプションを書いている間に終わる)。本体とサムネは
  `ImageBitmap` を1回だけ作って2回描き分ける(デコードが一番重いので2回やらない)

### ⑤ 初回に落ちてくる量(2026-08-22 追加)

①〜④は「往復回数」の話。ここは別の層で、**最初の1回に何バイト落ちてくるか**。
測り方は `.next/server/app/<route>.html` が参照しているチャンクを足すだけ
(2回目以降は `/_next/static/` が immutable キャッシュなので0バイト。効くのは初回と、
友達が初めて開くとき)。

- **Storage SDK を全ページから外した(-33KB)**。`lib/firestore.ts` が先頭で
  `firebase/storage` を静的 import していたため、`AuthProvider` → root layout と
  連鎖して**写真を1枚も扱わない画面(ホーム・2択・DM・カレンダー)にまで**乗っていた。
  実際に使うのは `uploadImage` ただ1つなので動的 import にした
  (`lib/firebase.ts` の `loadStorage`)。**静的 import に戻さないこと。**
- **同梱画像を再圧縮(632KB → 285KB、-55%)**。`public/seed/` の26着と
  `public/icons/` のPWAアイコン。中身はイラスト調で色数が少ないのに RGBA の
  フルカラーPNGだったので、パレットPNGに入れ替えた(sharp の
  `png({ palette: true, quality: 90, effort: 10 })`)。3倍に拡大しても差が分からない。
  **拡張子もファイル名も変えていない**——既存ユーザーの Firestore には
  `/seed/men/white-shirt.png` という文字列が保存済みなので、`.webp` に変えると
  既存のクローゼットが全部リンク切れになる。**同じ理由で、今後もここは改名しないこと。**
- **等幅Webフォントをやめた(-69KB)**。`Geist_Mono` はプロフィール編集画面の
  APIキー表示2箇所でしか使っていなかった。1ファミリー丸ごと配信する価値がないので
  `--font-mono` を端末標準の等幅に変えた(`globals.css`)。本文の `Geist` は残してある。

結果、ホーム(/feed)の初回は **JS 1359KB(gzip 405KB)+ フォント74KB + 画像289KB**。

**JSはこれ以上ほとんど削れない。** 内訳は Firebase の auth+firestore が約650KB、
react-dom が約250KB、Next のランタイムが残り。firestore を `firebase/firestore/lite` に
すれば激減するが、**`onSnapshot` が無くなる**ので2択の投票・DM・通知の即時反映が
全部死ぬ。割に合わないので採らない。
なお `lib/firestore.ts`(60KB)が共通チャンクに丸ごと入っているのは
**問題ではない**——どの実画面でも使うので、1つにまとめて共有・キャッシュさせるのが
正解。細かく分けると逆にリクエストが増える。分割しようとしないこと。

## 4.12 公式アカウントのプレビュー(2026-08-05 追加。Kazさん依頼)

**フォローが5人以下**(`PREVIEW_FOLLOW_THRESHOLD`)のあいだ、公式アカウントのコーデと
2択が**横に自動で流れる**帯をホームと2択一覧の先頭に出す。狙いは「空のタイムラインを
見せない」ことと「フォローの促進」。実装は `src/components/OfficialPreview.tsx`。

- **追加の読み込みはしない。**親がすでに購読している公開投稿・公開2択を、
  公式アカウントのUIDで絞り込むだけ。公式一覧だけは引くが `cachedOnce` で1回に抑える
- 自動スクロールは秒速22px。**指を置くと止まり**、端末が「動きを減らす」設定なら動かない。
  折り返しのため同じ並びを2周ぶん描き、半分を過ぎたら先頭へ戻す
- 各カードに `FollowChip`(その場でフォロー)。**`<a>` の中に `<button>` を置かない**よう、
  リンクとボタンは兄弟に並べてある
- 公式サンプルの2択は**期限を3日→30日**に延ばした。プレビューが数日で空になると
  初見の画面が寂しくなるため(再投入で賑わいを保つ運用は従来どおり有効)

## 4.13 APIキーの登録(2026-08-22。Kazさん依頼)

**AI合成のキーを、最初の登録画面でも入れられるようにした。** それまではサインイン後に
プロフィール編集まで辿り着かないと登録できず、実質「気づかない機能」になっていた。

### どこで入れられるか(2か所。どちらも同じ部品を使う)
- `src/app/onboarding/page.tsx` — サインイン**前**の画面。任意入力。
- `src/app/profile/edit/page.tsx` — 登録後、いつでも入れ替え・削除できる。

説明文・取得手順・入力欄は `src/components/ApiKeySetup.tsx` に集約してある。
**片方の画面だけ文言を直さないこと。** 登録時と設定時で説明が食い違うと利用者が混乱する。

### 未入力でも必ず登録できる
キーは任意。空のまま進んだ人には、入力欄の下と登録完了画面の両方で
`NoKeyNotice` を出す(「AI生成だけが使えない」「他は全部使える」「あとから足せる」)。
**登録を止めたり、後回しにさせたりしない。** キーの取得で離脱されるのが一番まずい。

### Google と OpenAI の両対応
提供元は**利用者に選ばせない**。貼られた文字列の先頭で機械的に判別する
(`AIza` → Google、`sk-` → OpenAI)。キーを取ってきた本人にとってどこで取ったかは
自明なので、選択肢を出しても手間が増えるだけ。

⚠ **判別規則は2か所にある。** `src/lib/aiProviders.ts` の `detectAiProvider` と
`functions/src/index.ts` の `detectProvider`。**片方だけ変えると、画面では受け付けたのに
サーバーが別の提供元として叩いて失敗する。** 必ず両方揃えること。

⚠ Firestore の `userSecrets/{uid}` のフィールド名は今も **`geminiApiKey`**。
Google 専用だった頃の名残で、中身は OpenAI のキーのこともある。
**改名すると既存利用者の登録が全部読めなくなる**ので、`provider` と組で扱うこと。
`provider` を持たない古い登録は、キーの形から割り出して補う実装が両側に入っている。

### サーバー側の作り
`composeWithAi` が入口で、`generateWithGemini` / `generateWithOpenAI` に振り分ける。
指示文(`COMPOSE_INSTRUCTION`)と保存(`saveComposedImage`)は共通。
- Google: `generateContent` に inlineData を並べる。`responseModalities` に TEXT も要る。
- OpenAI: `POST /v1/images/edits` に multipart で `image[]` を繰り返す(最大16枚)。
  **顔を必ず先頭にする**——OpenAI は1枚目を主たる編集対象として扱うため。
  返りは `data[0].b64_json`。

キャッシュキーにモデル名を混ぜてあるので、提供元を乗り換えた人は自然に作り直しになる。

### 確認機能(`verifyAiKey`)
旧 `verifyGeminiKey` を改名したもの。**1枚生成して確かめると本人に課金が発生する**ので、
確認は「そのモデルを引けるか」までにとどめている(GET でモデル情報を取るだけ)。
これでキーの有効性と権限は分かる。キーの中身はクライアントに返さない。

### 検証状況
Google 側は以前から実データで成功を確認済み。**OpenAI 側は Kazさんがキーを持っていない
ため未検証**(公式ドキュメントの仕様どおりに実装してある)。誰かが OpenAI のキーで
試したら、結果をここに追記すること。

## 5. 実装していないもの(と理由)

| 機能 | 理由 |
|---|---|
| 指定時刻のプッシュ通知 | 設定UIのみ。実際のpushはFCM+Service Worker+Cloud Schedulerが必要で、iOS Safariの制約もあり無料枠では安定しない |
| 流行りを取り込んだレコメンド | 外部データ源が必要。代わりに季節・好みのジャンル・着ていない期間から選ぶローカル実装(`src/lib/recommend.ts`)。**外部API不使用なので費用ゼロ** |
| モデル/芸能スカウト機能 | 認証バッジ(`official`)まで実装済み。残りはアカウント種別と連絡導線の設計 |
| PWAオフライン対応 | Service Worker未実装。**意図的に見送っている**(8章「見送りの判断基準」参照) |
| 天気の定時通知 | 天気の取得自体は実装済み(4.7)。定時プッシュは上記と同じ理由で未実装 |

> 「投稿の編集」は 2026-08-03 に実装済み(4.7)。
> 「DMでの写真送信」は当初テキストのみの方針だったが、**Kazさんの依頼により 2026-08-04 に実装済み**(3章のDM参照)。

---

## 6. 開発時の実務メモ

### 環境
- **`node` / `npm` にPATHが通っていない。** 毎回これを付ける:
  ```powershell
  $env:PATH = "C:\Users\kazdr\AppData\Local\nodejs-portable\node-v24.18.1-win-x64;" + $env:PATH
  ```
- Bashツールからは `npx` が見えない。PowerShell経由で実行すること。
- コミットメッセージは Bash ツールならヒアドキュメント(`<<'EOF'`)。**PowerShellの `@'...'@` は Bash では動かない**(一度失敗した)。

### デプロイ
- Web: `git push origin master` → Vercel が自動デプロイ(約2〜3分)。
- ルール: `firebase deploy --only firestore:rules,storage`
- **索引: `firebase deploy --only firestore:indexes`**(2026-08-05 追加。作成は非同期で、
  小さなデータなら数分。作成中のクエリは「building」として失敗し続ける)
- 関数: `firebase deploy --only functions`

### ⚠ 反映確認でVercelの防御を作動させた事故
デプロイ確認のため15秒間隔で40回ポーリングしたところ、**Vercelの自動DDoS緩和が作動して自分のIPが403になった**
(サイト全体ではなく接続元のみ。他IPからは正常だった)。30分ほどで自動解除。
**確認は数回に留めること。** ルートのHTTPステータス(404→200)か、JSバンドル内の文字列検索を1回行えば十分。

### 検証で使える手段
- Firestoreの実データはgcloudのアクセストークン+REST APIで読める(原因特定に有効だった)。
  ```powershell
  $t = gcloud auth print-access-token
  Invoke-RestMethod -Uri "https://firestore.googleapis.com/v1/projects/my-clothes-46c81/databases/(default)/documents/users?pageSize=50" -Headers @{Authorization="Bearer $t"}
  ```
- **クエリが索引不足で落ちていないかは、REST の `:runQuery` を叩けば1発で分かる。**
  「遅い」と言われたら、まずこれで**そもそも成功しているか**を確かめること
  (2026-08-05 に、遅いのではなく失敗し続けていたことが判明した)。
  ```powershell
  $env:GTOKEN = (gcloud auth print-access-token)
  # 例: 公開タイムラインのクエリが通るか
  #   POST https://firestore.googleapis.com/v1/projects/my-clothes-46c81/databases/(default)/documents:runQuery
  #   body: {"structuredQuery":{"from":[{"collectionId":"stylePosts"}],
  #           "where":{"fieldFilter":{"field":{"fieldPath":"visibility"},"op":"EQUAL",
  #                                   "value":{"stringValue":"public"}}},
  #           "orderBy":[{"field":{"fieldPath":"createdAt"},"direction":"DESCENDING"}],"limit":5}}
  # 索引が無ければ 400 と「作成リンク」が返る。あれば結果が返る。
  ```
- **画像の配信ヘッダは1コマンドで確認できる**(キャッシュが効いているかの確認):
  ```powershell
  python -c "import urllib.request;r=urllib.request.urlopen('<画像URL>');print(r.headers.get('Cache-Control'))"
  ```
- **ログイン後の画面はClaude側から操作できない。** 実機確認は必ずKazさんに依頼すること。

---

## 7. ディレクトリ構成(抜粋)

```
src/
  app/
    feed/          公開タイムライン(ホーム)
    vote/          2択の一覧と詳細(投票・着用確定)
    create/        コーデ作成(決め方2種)
    closet/        クローゼット、closet/add で登録
    post/new/      全身写真の投稿(アイテムタグ付け)
    post/[postId]/ 投稿詳細+コメント
    profile/       マイページ、profile/edit で編集(アイコン変更もここ)
    u/[uid]/       他人のプロフィール(フォロー・DM導線)
    calendar/      1ヶ月のコーデ記録
    chat/          DM一覧、chat/[threadId] で会話
    search/        ユーザー検索+ジャンル別グリッド
    upgrade/       プラン説明・申し込み・解約
    legal/tokushoho/ 特商法表記(未記入)
  components/
    ui.tsx         共通UI(ActionBar, Avatar, Chip, BottomSheet ほか)
    OutfitCard.tsx 合成画像 or 顔+服のボード表示
    HangerRail.tsx ハンガー表示
    StylePostCard.tsx 公開投稿カード(アイテムタグ表示)
    AppShell.tsx   認証ガードと下タブ
  lib/
    firestore.ts   データアクセス全般
    recommend.ts   ローカル提案ロジック(外部API不使用)
    functions.ts   Cloud Functions 呼び出し(リージョン定数に注意)
    weather.ts     天気の取得と助言(Open-Meteo / APIキー不要)
    share.ts       共有・写真の保存・JSON書き出し(段階的に落とす作り)
  components/
    WeatherBar.tsx    今日の天気と助言
    SuggestedUsers.tsx おすすめの人(骨格・カラーが近い人)
functions/src/
  index.ts         composeOutfitImage(Gemini合成。プロフィールで個別化)
  billing.ts       Stripe(デプロイ済み)
```

あとから追加した主なルート: `/activity`(通知)、`/tag/[tag]`(ハッシュタグ)。

---

## 8. 次のセッションで最初にやるとよいこと

1. **このファイルを読む。**課金まわりだけ触るなら `BILLING_SETUP.md` も。
2. **Kazさんに実機の確認結果を聞く。**「0. まず把握すべき現状」の5項目が未検証。
   Claude側からログイン後の画面は操作できないので、ここは必ず本人に依頼すること。
3. 不具合を追うときは**推測せず実データを見る。**Firestore は gcloud のトークン + REST で読める
   (6章)。過去に「症状から推測して誤った説明をした」事故があり、実データを見て解決している。

### 未実装のまま残っているもの(優先順位つき)

Kazさんに10案を提示し、**7つを実装済み**。残り3つと理由は以下。

| 案 | 状態 | 判断 |
|---|---|---|
| **ログイン不要の公開ビュー + OGP画像** | 未 | **今の設計では作れない。**全ルートが認証必須で、サーバー側から投稿を読む経路が無い。リンクを外部に貼っても中身が見えないので、**外部流入を作るならここが最優先**。公開ビューを先に作る必要がある |
| **フィードのページングと並び順** | 未 | 今は `watchPublicStylePosts` が最新100件固定。**利用者が増えると新規投稿者が誰の目にも触れなくなる**(=投稿が止まる)。壊れるのは伸び始めてからなので急ぎではないが、影響は大きい |
| **DMの未読・既読** | **済(2026-08-04)** | Kazさんの依頼で実装。設計は当初のメモどおり `lastReadAt: {uid: number}`。送信時刻表示・写真送信も同時に実装 |
| アイテムページ(ブランド×商品の集約) | 未 | アフィリエイト収益はここで出る。人が集まってからで間に合う |

### 見送りの判断基準として使ってきたもの

- **後戻りできないものは、許可があっても勝手に入れない。**
  例: Service Worker(オフライン対応)。壊れたSWは利用者の端末に残り続け、
  こちらから復旧できない。「後で消せばいい」が成立しないため見送っている。
- **今動いているもの(課金・AI合成)を壊す変更は単独で扱う。**
  例: 1日1回制限のCloud Functions化。整合性は上がるが、決済が通ったばかりの
  投稿経路を作り替えることになるので、リスクに見合わないと判断した。

### 実装するときに踏みやすい地雷(再掲)

1. **ネオン/アクセント色を文字に使わない**——と同種の失敗として、**`#EBFC34` のような
   地色と明度の近い色を文字色にすると読めない。**現行は青 `#3b6bff` なので問題ないが、
   配色を変えるときは必ずコントラストを確認すること。
2. **画面下の固定要素は `ActionBar` を使う。**素で `bottom-0` に置くと下タブの裏に隠れる。
3. **Firestoreのルールで、存在しないドキュメントの `resource` は null になる。**
   新しいコレクションを足すときは「存在しないドキュメントを読む経路があるか」を必ず確認。
4. **クライアントとCloud Functionsのリージョンを一致させる**(どちらも `us-central1`)。
5. **シークレットを更新したら必ず再デプロイ。**関数はデプロイ時点のバージョンを掴む。
6. **ハッシュタグは `normalizeHashtag()` を必ず通す。**通さないと表記ゆれで投稿が分断される。
7. **Storage の CORS 設定を消さない。**消すと「写真を保存」が壊れる。
8. デプロイ反映の確認は数回に留める(過剰なポーリングで Vercel の防御が作動した実績あり)。
9. **`onSnapshot` にエラーコールバックを必ず渡す**(`snapshotFailed` を使う)。
   省くと索引不足・権限エラーが**画面上「永久に読み込み中」にしか見えず**、
   原因が「遅い」としか観測できない。実際に長期間これで潰れていた。
10. **equality + orderBy / collectionGroup のクエリを足したら
   `firestore.indexes.json` を更新してデプロイする。**
11. **画像は必ず `uploadImage` 経由で上げる。**直に `uploadBytes` すると
   `cacheControl` が付かず、ブラウザにキャッシュされない。
12. **一覧・グリッドの画像は `thumbSrc(post)` を使う。**`post.imageUrl` を直接読むと
   1280px の本体を3列グリッドに並べることになる。
13. **collectionGroup を使うときは、索引と `{path=**}` のルールをセットで用意する。**
   固定パスのルールは collectionGroup クエリに適用されない。
